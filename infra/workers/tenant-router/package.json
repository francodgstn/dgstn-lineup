{
  "name": "linyup-tenant-router",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker: routes tenant custom domains onto the public route tree",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250801.0",
    "typescript": "^5.9.2",
    "wrangler": "^4.28.0"
  }
}
