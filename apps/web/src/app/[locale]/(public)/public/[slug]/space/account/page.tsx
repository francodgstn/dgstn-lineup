import SpaceShell from '../SpaceShell'
import AccountHome from './AccountHome'

export const dynamic = 'force-dynamic'

export default function SpaceAccountPage() {
  return (
    <SpaceShell>
      <AccountHome />
    </SpaceShell>
  )
}
