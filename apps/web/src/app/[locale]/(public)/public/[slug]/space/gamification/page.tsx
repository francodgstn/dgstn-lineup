import SpaceShell from '../SpaceShell'
import GamificationHome from './GamificationHome'

export const dynamic = 'force-dynamic'

export default function SpaceGamificationPage() {
  return (
    <SpaceShell>
      <GamificationHome />
    </SpaceShell>
  )
}
